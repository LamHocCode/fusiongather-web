import Container from "./Container";

const Sider = ({ children }: { children: React.ReactNode }) => {
    return (
        <Container>
            {children}
        </Container>
    );
}

export default Sider;